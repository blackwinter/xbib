
dependencies {
    compile project(':datastructures:metrics')
}

test {
    useJUnit {
        forkEvery 1
        testLogging {
            showStandardStreams = false
            exceptionFormat = 'full'
        }
    }
}
